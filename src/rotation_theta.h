#ifndef ROTATION_THETA_H
#define ROTATION_THETA_H

#include <math.h>

extern void rotation_theta(float theta0, float psi0, float psiO,
                           float dtheta, float *theta, float *psi);

#endif
